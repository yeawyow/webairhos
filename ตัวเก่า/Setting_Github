git --version <= เช็ค Version ของ Github
git config --global user.email "" <= ตั้งค่า E-mail ของตัวเอง
git config --global user.name "" <= ตั้งค่า Name ของตัวเอง
git config --list <= เป็นการเช็คว่าเราได้ตั้งค่า E-mail และ Name หรือยัง
git clone "LINK" <= เป็นการ Clone ที่อยู่ที่จะเก็บ Code โปรแกรมไว้
git init <= เป็นการสร้างไฟล์ Fig เพื่อเอาไว้เชื่อมต่อกับ Github
git add . <= เป็นการเพิ่มข้อมูลทั้งหมดเข้าไปยัง Github
git commit -m "Text" <= เป็นการกำหนดข้อความเอาไว้ว่าเราแก้ไข Code อะไรบ้าง
git branch -M main <= เป็นการ Branch ว่าให้อยู่ใน Branch ไหน
git push -u origin main <= เป็นการนำเข้า